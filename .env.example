# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Showrunner LLM (story engine, schema-validated JSON)
ANTHROPIC_API_KEY=

# Video generation — MiniMax H3 Max. fal-exclusive; get a key at fal.ai
FAL_KEY=

# Reference plates / location plates — bytedance/seedream-4
REPLICATE_API_TOKEN=

# Per-character dialogue TTS
ELEVENLABS_API_KEY=
